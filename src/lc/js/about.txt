MUST — ćwicz najpierw
--debounce — closure + setTimeout
--throttle — odpala co X ms, nie po ciszy
--memoize — closure + Map
--Promise.all — wszystkie, kolejność, odrzuca na pierwszym błędzie
--deepClone — rekurencyjna kopia
--flatten — spłaszcz zagnieżdżoną tablicę
--curry — add(1)(2)(3)
--EventEmitter — on/emit/off (pub/sub)
SHOULD — warto mieć
--Promise.race — pierwszy wygrywa
--Promise.allSettled — wszystkie, statusy, nie odrzuca
--retry(fn, times) — ponów przy błędzie
--bind (własny) — this + argumenty
--deepEqual — porównanie głębokie
--groupBy — pogrupuj wg klucza
--własny map/filter/reduce — iteracja + callback
--sleep/delay — Promise + setTimeout
--parallelLimit — max K promisów naraz
--Bonus algo — zrób te 3 (rozgrzewka)
--frequency counter — policz wystąpienia (Map)
--two sum — dwie liczby do target (Map)
--anagram check — czy anagramy


Tablice — iteracja:

js
arr.map(x => x * 2)
arr.filter(x => x > 5)
arr.reduce((acc, x) => acc + x, 0)
arr.forEach(x => ...)
arr.find(x => x === 5)             // element
arr.findIndex(x => x === 5)        // indeks
arr.some(x => x > 5)               // boolean — któryś?
arr.every(x => x > 5)              // boolean — wszystkie?
arr.includes(5)                    // zawiera
arr.indexOf(5)                     // indeks lub -1


Tablice — modyfikacja:

js
arr.push(x) / arr.pop()            // koniec: dodaj / usuń
arr.unshift(x) / arr.shift()       // początek: dodaj / usuń
arr.slice(1, 3)                    // wytnij fragment (nie mutuje)
arr.splice(1, 2)                   // usuń 2 od indeksu 1 (MUTUJE)
arr.concat(arr2)                   // połącz
arr.sort((a, b) => a - b)          // sortuj rosnąco (MUTUJE)
arr.reverse()                      // odwróć (MUTUJE)
arr.flat()                         // spłaszcz o 1 poziom
arr.fill(0)                        // wypełnij
Array.from({length: 5}, (_, i) => i)   // [0,1,2,3,4]
Array(5).fill(0)                   // [0,0,0,0,0]


Tablice — liczenie (reduce):

js
arr.reduce((sum, x) => sum + x, 0)                          // suma
arr.reduce((acc, x) => { acc[x] = (acc[x]||0)+1; return acc }, {})  // częstotliwość
arr.reduce((acc, x) => { (acc[x.type] ||= []).push(x); return acc }, {})  // grupowanie
Math.max(...arr) / Math.min(...arr)


Stringi:

js
str.length
str.toLowerCase() / toUpperCase()
str.includes('abc') / indexOf('abc')
str.charAt(0) / str[0]
str.charCodeAt(0)                  // kod znaku (do sortowania/porównań)
str.slice(0, 5) / substring(0, 5)
str.split('') / split(' ')         // → tablica
arr.join('') / join('-')           // → string
str.trim()
str.replace('a', 'b') / replaceAll('a', 'b')
str.repeat(3)
str.startsWith('a') / endsWith('z')
str.padStart(3, '0')               // "5" → "005"
[...str].reverse().join('')        // odwróć


Liczby / matematyka:

js
Math.floor(x) / ceil(x) / round(x)
Math.abs(x)
Math.max(a, b) / Math.min(a, b)
Math.random()
Math.floor(Math.random() * n)      // losowa 0..n-1
Math.pow(2, 3) / 2 ** 3            // potęga
Math.sqrt(x)
x % 2 === 0                        // parzysta
Number(str) / parseInt(str) / parseFloat(str)
x.toFixed(2)                       // "3.14" (string!)
Number.isInteger(x)
(123).toString(2)                  // do binarnego
parseInt("1010", 2)               // z binarnego


Obiekty:

js
Object.keys(obj)                   // klucze
Object.values(obj)                 // wartości
Object.entries(obj)                // [[k, v], ...]
Object.assign({}, obj)             // płytka kopia
Object.fromEntries(entries)        // [[k,v]] → {}
'key' in obj
obj.hasOwnProperty('key')
delete obj.key

Map / Set:

js
const m = new Map()
m.set(k, v); m.get(k); m.has(k); m.delete(k); m.size
for (const [k, v] of m) {...}      // iteracja

const s = new Set(arr)             // unikalne
[...new Set(arr)]                  // usuń duplikaty z tablicy
s.has(x); s.add(x); s.delete(x); s.size

Pętle:

js
for (let i = 0; i < arr.length; i++) {...}
for (const x of arr) {...}          // wartości (tablica/string/Set/Map)
for (const k in obj) {...}          // klucze (obiekt)
while (cond) {...}